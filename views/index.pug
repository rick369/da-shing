doctype
html
  head
    meta(charset="UTF-8")
    title Init Project Web
    script!= 'window.INITIAL_STATE = ' + JSON.stringify(initialState) + ';'
  body
    #app!= componentHTML
    script(src="/build/bundle.js")
